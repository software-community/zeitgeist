var typed = new Typed('.main-heading', {
    // Waits 1000ms after typing "First"
    strings: ['ZEITGEIST 2k21', 'Techno School Program'],
    typeSpeed: 100,
    smartBackspace: true,
    loop: true,
    backDelay: 2000,
});